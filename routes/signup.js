var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res, next) {
  res.render('index', { title: 'PlantR' });
});
<input type="password" size="10" name="txtpwd" id="txtpwd" value="">

module.exports = router;
